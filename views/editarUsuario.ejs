<!DOCTYPE html>
<html lang="en">

  <%- include('./partials/head', {stylesheet: 'mi_cuenta.css', stylesheet2: 'editarUsuario.css',title: 'Editar informacion'}) %> 

<body>
  <%- include('./partials/header', {view: 'editarUsuario'}) %> 
  <main>
    <div class="img-container">
      <img class="main-img" src="/images/main-header.jpg" alt="">
    </div>
    <div class="forms-container">
      <div class="forms-carousel">
        <form class="sign-up-form" action="/usuario/editarUsuario" method='post'>
          <h3>Mis datos</h3>
          <div class="input-container">
            <i class="fas fa-user"></i>
            <label for="name"></label>
            <input type="text" name="name" id="name" placeholder="Nombre" value= "<%= locals.user ? locals.user.name : "" %>"  required>
          </div>
          <div class="input-container">
            <i class="fas fa-user"></i>
            <label for="last-name"></label>
            <input type="text" name="lastName" id="last-name" placeholder="Apellido" value= "<%= locals.user ? locals.user.lastName : "" %>" required>
          </div>
          <div class="input-container">
            <i class="fas fa-envelope"></i>
            <label for="email"></label>
            <input type="email" name="email" id="email" placeholder="Correo electrónico" value= "<%= locals.user ? locals.user.email : "" %>" required>
          </div>
          <div class="input-container">
            <i class="fas fa-map-marked-alt"></i>
            <label for="address"></label>
            <input type="text" name="address" id="address" placeholder="Direccion" value= "<%= locals.user ? locals.user.address : "" %>" required>
          </div>
          <div class="input-container">
            <i class="fas fa-mobile"></i>
            <label for="phone"></label>
            <input type="text" name="phone" id="phone" placeholder="Telefono" value= "<%= locals.user ? locals.user.phone : "" %>" required>
          </div>
          <div class="input-container">
            <i class="fas fa-lock"></i>
            <label for="password"></label>
            <input type="password" name="password" id="password" placeholder="contraseña" required>
          </div>
          <div class="btns">
            <a class="sign-up-btn" href="#" onclick="showDeletePopUp()">Eliminar Cuenta</a>
            <input class="submit-btn" type="submit" value="Guardar">
          </div>
        </form>
      </div>
    </div>
    <div class="deletePopUp">
      <h2>Eliminar cuenta</h2>
      <p>Esta seguro que desea eliminar su cuenta?</p>
      <small>Esta accion no puede dehacerse.</small>
      <form action="/usuario/eliminar/:<%= locals.user.id %> " method="post">
        <label for="password">Ingrese su contrasena para continuar:</label>
        <input class="input-container" type="password" name="password" id="password">
        <button class="sign-up-btn" type="submit" >Eliminar</button>
      </form>
    </div>
  </main>
  <%- include('./partials/footer') %> 
  <script src="/js/deletePopUp.js"></script>
</body>

</html>